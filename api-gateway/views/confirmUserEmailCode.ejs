<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="stylesheet" href="/public/css/styles.css">
		<title>Confirm User Email Code</title>
	</head>
	<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-[#020617] via-[#0a1a2f] to-[#0f172a]" style="animation: bgPulse 6s ease-in-out infinite;">
                <div class="z-10 relative bg-white/50 backdrop-blur-sm rounded-xl shadow-md p-6">
		<main>
			<header>
				<a href="/home">Home</a>
			</header>
			<section>
				<h2>Confirm User Email Code Page</h2>
			</section>
			<section>
				<% if (error && error.length > 0) { %>
				<% error.forEach(err => { %>
					<p style="color: red"> <%= err %> </p>
				<% }) %>
				<% } %>
				<form action="/validateUserEmailCode" method="POST">
					<label>
						Code:
					<input type="text" name="captchaInput" placeholder="Type the code here" required>
					<button type="submit">Submit</button>
					</label>
				</form>
			</section>
			<footer>
				<h6>Time do balacobaco</h6>
			</footer>
		</main>
		</div>
	</body>
</html>
